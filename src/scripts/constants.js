// Default board size
export const DEFAULT_SIZE = { rows: 5, columns: 8 };

// Maximum board size
export const MAX_SIZE = { rows: 8, columns: 12 };

// Walking frequency, in ms
export const INTERVAL = 750;

// Board square width
export const SQUARE_WIDTH = 64;
